import { ExtractCategoriesPipe } from './extract-categories.pipe';

describe('ExtractCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
